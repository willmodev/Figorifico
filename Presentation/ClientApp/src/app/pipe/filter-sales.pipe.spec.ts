import { FilterSalesPipe } from './filter-sales.pipe';

describe('FilterSalesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSalesPipe();
    expect(pipe).toBeTruthy();
  });
});
