import { FilterInvoicePipe } from './filter-invoice.pipe';

describe('FilterInvoicePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInvoicePipe();
    expect(pipe).toBeTruthy();
  });
});
